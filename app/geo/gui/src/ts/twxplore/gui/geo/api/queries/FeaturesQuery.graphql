query FeaturesQuery($aQuery: FeatureQuery!, $aLimit: Int!, $aOffset: Int!){
	features(limit:$aLimit, offset:$aOffset, query:$aQuery){
    label
    type
    uri
    geometry{
      label
      wkt
      uri
    }
  }
}