query FeaturesQuery($query: FeatureQuery!, $limit: Int!, $offset: Int!) {
  features(limit: $limit, offset: $offset, query: $query) {
    label
    type
    uri
    geometry {
      label
      wkt
      uri
    }
  }
}
